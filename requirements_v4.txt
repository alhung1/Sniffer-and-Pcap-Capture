# WiFi Sniffer Web Control Panel v4 - Dependencies
# Install with: pip install -r requirements_v4.txt
#
# v4 NOTE: paramiko is NO LONGER required.
# We use native Windows OpenSSH (subprocess) instead.

# Core dependencies
flask>=2.3.0

# WebSocket support for real-time updates
flask-socketio>=5.3.0

# Async support (optional, improves SocketIO performance)
eventlet>=0.33.0
